action: drain_battery
object_type: flashlight

parameters:
  # No parameters needed

preconditions:
  # No preconditions

effects:
  # Drain battery to empty
  - type: set_attribute
    target: battery.level
    value: "empty"
